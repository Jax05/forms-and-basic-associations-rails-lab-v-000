<%= form_for @song do |f| %>
  <%= f.label :title %><br />
  <%= f.text_field :title %>

  <%= f.label :artist_name %><br />
  <%= f.text_field :artist_name %>

  <%= f.collection_select :song_genre_id, Genre.all, :name, :name %><br />

  <input name="song[song_notes_ids][]">
  <input name="song[song_notes_ids][]">
  <input name="song[song_notes_ids][]">

  <%= f.submit %>
<% end %>
